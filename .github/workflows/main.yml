name: King Arthur
on:
  workflow_dispatch:

jobs:
  sword-pulling:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repos code
        uses: actions/checkout@v3

      - name: the excalibur
        env:
          THE_SECRET: ${{ secrets.API_KEY }}
        run: python t.py

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run script
        env:
          # This maps the GitHub Secret to an Environment Variable
          API_KEY: ${{ secrets.API_KEY }} 
        run: python my_script.py
